import arrFilter from '../../../utils/arr-filter';
import { module, test } from 'qunit';

module('Unit | Utility | arr filter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrFilter();
  assert.ok(result);
});
