import { arrLength } from '../../../helpers/arr-length';
import { module, test } from 'qunit';

module('Unit | Helper | arr length');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrLength([42]);
  assert.ok(result);
});
