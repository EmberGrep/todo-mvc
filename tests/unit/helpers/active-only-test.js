import { activeOnly } from '../../../helpers/active-only';
import { module, test } from 'qunit';

module('Unit | Helper | active only');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = activeOnly([42]);
  assert.ok(result);
});
